source ~/.shell-common.sh

# --- Google Cloud SDK - note that there is a parallel set in .zshrc ---
# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/ryan/google-cloud-sdk/path.bash.inc' ]; then . '/Users/ryan/google-cloud-sdk/path.bash.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/ryan/google-cloud-sdk/completion.bash.inc' ]; then . '/Users/ryan/google-cloud-sdk/completion.bash.inc'; fi
